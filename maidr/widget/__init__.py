"""Framework integrations for MAIDR.

Each submodule targets one web framework and requires that framework's
optional extra.  Nothing is imported here, so ``maidr.widget`` stays
importable whether or not any of them is installed.
"""
